apiVersion: sourceplane.io/v1
kind: Component

metadata:
  name: identity-worker

spec:
  type: cloudflare-worker-turbo
  domain: platform-identity
  subscribe:
    environments:
      - production
  inputs:
    workspaceDir: ../..
    appDir: apps/identity-worker
    installCommand: pnpm install --no-frozen-lockfile
    buildCommand: pnpm turbo run build --filter=@example/identity-worker
    typecheckCommand: pnpm turbo run typecheck --filter=@example/identity-worker
    deployCommand: pnpm --filter @example/identity-worker deploy
    nodeVersion: "20"
    pnpmVersion: "10.12.1"
    productionBranch: main
  labels:
    team: platform
    layer: runtime
    surface: identity
    runtime: cloudflare